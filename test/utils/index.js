const createARGV = require('./create-argv')
const loggerStub = require('./logger-stub')
const pinoStub = require('./pino-stub')
const setConfig = require('./set-config')

module.exports = {
  createARGV,
  loggerStub,
  pinoStub,
  setConfig
}
